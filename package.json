{
  "name": "aurora-blocks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ polarisfinance/aurora-blocks",
    "create-local": "graph create --node http://localhost:8020/ polarisfinance/aurora-blocks",
    "remove-local": "graph remove --node http://localhost:8020/ polarisfinance/aurora-blocks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 polarisfinance/aurora-blocks",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
